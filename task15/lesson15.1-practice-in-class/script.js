/* let country = "Sweden"; //Входные данные (что нунжо проверять)
let access = country == "Sweden" ? "True" : "False"; //проверка

console.log(access);


let num = 10;
for (let i = 0; i < 10; i = i + 1) {
    num++
}
 */

/* function checkNumInInterval (number, min, max) {
    if(number > min && number < max){
        console.log(number + "Находиться в диапазоне ");
    }
    else{
        console.log(number + "Не находиться в диапазоне");
    }
}

checkNumInInterval(2, 3, 8); */
